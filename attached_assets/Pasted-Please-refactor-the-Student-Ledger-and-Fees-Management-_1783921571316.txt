Please refactor the Student Ledger and Fees Management user experience from a popup modal into a dedicated full-screen profile page, and fix the main Transaction History module.

1. ROUTING & TRANSITION:
   - Convert the 'Full Timeline' action into a navigation link that opens a dedicated student dashboard route (e.g., /students/[id]). 
   - Keep the clean left-side summary panel (showing the student profile, photo placeholder, outstanding balance, and Arrears B/F) pinned to the left or top of the new page layout.

2. FULL-SCREEN TABS ARCHITECTURE & SMART DISTRIBUTION TRACKING:
   - Move the Financial Statement Feed and the 'Store Purchases' list into a spacious, full-screen tabbed interface on the right side of the page.
   - Give the item rows plenty of horizontal space so the item names, reference numbers, date stamps, status badges, and action buttons look clean and uncrowded.
   - STATUS TRACKING & STOCK CHECK: Add an inventory distribution tracking status badge to every item in the store logs: "Pending Collection" or "Issued" (with a timestamp and staff name). If an item is "Pending Collection", check warehouse availability. If stock > 0, show a green "Issue" button; if stock is 0, display a yellow "Out of Stock" warning badge and disable manual issuance.
   - BULK ACTION FEATURE: Add a prominent "Issue All Available Items" bulk action button at the top of the checklist for manual overrides or partial pick-ups. Clicking this must instantly transition all currently in-stock pending items for this student to "Issued" in a single batch operation.

3. PAYMENT FLOW AUTO-ISSUANCE WORKFLOW:
   - On the bundle/fees payment confirmation view, include a workflow option checkbox: "Hand over available items right now" (which must be checked by default). 
   - If this checkbox is active when the payment is processed, the UI must immediately reflect that all available bundle books have transitioned directly to "Issued" with the cashier's signature, completely eliminating the need for the cashier to navigate to another tab to manually click "Issued".

4. FIX MAIN TRANSACTION HISTORY MODULE METRICS:
   - 'Store Purchases/Sales' card must ONLY display the sum of transactions categorized as Type: 'Store'.
   - 'Fees Collected' card must display the sum of ALL tuition, acceptance fees, and bundle overhead balance line items (Type: 'Bundle' and 'Acceptance').
   - Add a Tab Bar right below the summary cards to filter the main ledger view: [All Transactions] [Store Sales] [School Fees & Bundles].

5. ROLE-BASED ACCESS CONTROL (RBAC) SECURITY UI:
   - Restrict interface actions based on logged-in user roles:
     * Owners/Admins: Full access to modify transaction histories, adjust outstanding balances, issue refunds, and view overall school fee revenue.
     * Storekeepers/Cashiers: Restricted view. They are strictly blocked from editing prices, deleting transaction logs, or viewing master tuition/fee revenue totals on the main summary cards.