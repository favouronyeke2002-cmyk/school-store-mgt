Please implement the backend business logic, API endpoints, and database updates to handle inventory deduction, automated bundle distributions, and Role-Based Access Control (RBAC) enforcement.

1. COUPLING PAYMENT WITH AUTO-ISSUANCE LOGIC:
   - Modify the bundle/registration payment processing endpoint to look for the "Hand over items right now" flag.
   - If the flag is TRUE: The system must execute a single atomic database transaction that simultaneously logs the financial payment AND automatically processes the inventory deduction for all package items currently in stock, bypassing "Pending" and creating them directly as "Issued" with the cashier's timestamp and user ID.
   - If the flag is FALSE (or items are out of stock): Populate those specific book rows in the student's ledger profile database as "Pending Collection" without reducing the warehouse stock count yet.

2. STOREKEEPER ACTION & DYNAMIC STOCK SYNC:
   - Create a secure API endpoint to handle the "Mark as Issued" or "Issue All Available" backend action.
   - When executed by authorized staff, the logic must:
     * Flip the target item tracking status from "Pending Collection" to "Issued".
     * Automatically append a backend timestamp and the exact username of the active staff member session.
     * DEDUCT the exact quantity from the physical Inventory database table immediately to preserve inventory asset valuation accuracy.

3. REVENUE ISOLATION & BALANCING:
   - Ensure that the cost price of items distributed via registration packages is linked against institutional 'Fees Collected' revenue records, preventing bundle inventory costs from contaminating or lowering the retail profit margins calculated inside the 'Store Purchases/Sales' charts.

4. BACKEND RBAC ENFORCEMENT:
   - Secure all financial and inventory API routes using active user session roles:
     * Reject any requests with a 403 Forbidden error to delete transaction logs, edit book prices, or fetch total school fee revenue summaries if the user's role is 'Storekeeper' or 'Cashier'.
     * Allow 'Storekeeper' and 'Cashier' roles to successfully execute the stock issuance and bundle distribution tracking endpoints.